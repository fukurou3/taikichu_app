、「10万MAUで盤石な黒字事業を確立し、価値あるデータを蓄積すること」に再設定する。

フェーズ0〜2に全資源を集中: 10万MAUまでのロードマップを徹底的に実行します。ご指摘のあった5つの即実行タスク（コストシート自動化、TLロジック検証、Redis PoCなど）を最優先で開始します。

技術的ゴールをビジネス的ゴールに変換: 「10万MAUを達成し、インフラ費を売上の15%以下に抑え、ARPU X円を達成した安定した事業体」を作り上げることを目標とします。

フェーズ2完了時点を「運命の分岐点」と位置づける: この目標を達成した時点で、手元にある「黒字事業」と「10万人分の行動データ」を最大の武器として、次の選択を行います。

A) 資本注入（シリーズA）: 100万MAU以上を目指すためのSRE人件費や高額なインフラ費用を賄うため、本格的な資金調達に動く。

B) 事業売却（M&A）: 安定した黒字事業体として、より大きなシナジーを見込める企業への売却を検討する。



# フェーズ0 ― 決定版アーキテクチャ  
―― 1 万 MAU までを **月額 ≤ ¥10,000** で運用しながら、価値検証と黒字化を同時達成する “生存ライン” ――  

---

## 1. 目的と達成条件

| 区分 | 指標 | 閾値・判定 |
|------|------|-----------|
| **コスト** | 日次請求額 | **¥500 超**でメール即時アラート |
|           | 月次請求額 | **¥10,000 超**で *新機能凍結＋コスト削減スプリント* |
| **UX (p95)** | `GET /timeline` | **< 500 ms** |
|              | `POST /like` / `POST /post` | **< 150 ms** |
| **撤退** | DAU/MAU | **< 30 %** かつ 3 か月連続赤字 → プロジェクト終了 |

> **補強 KPI**  
> - **Day-1 / Day-7 / Day-30 Retention** を BigQuery で Cohort 計測。  
>   - Day-1 ≥ 35 %、Day-7 ≥ 20 %、Day-30 ≥ 10 % を目安。  

---

## 2. 採用コンポーネント

| レイヤ            | サービス & 主要設定 | 役割・備考 |
|-------------------|--------------------|------------|
| **フロント**      | Firebase Hosting + Cloud CDN | 静的 HTML/JS を配信。<br>画像は Cloud Storage オリジンを直接参照。 |
| **API**          | Cloud Run モノリス<br>• 同時実行 **80**<br>• **min_instances = 1** (1 vCPU / 512 MiB) | コールドスタート解消。アイドル課金 ≈ **$10/月 ≒ ¥1,500** |
| **DB**           | Cloud Firestore (tokyo)<br>• **limit 100 + cursor** を厳守 | 全データ格納。<br>日次 read 課金 **¥300 超 ×3日** で Fan-out 方式検討。 |
| **認証**         | Firebase Auth | メール / Google / X。SMS 無効。 |
| **ストレージ**   | Cloud Storage (Multi-Region)<br>• プリサイン URL<br>• `Cache-Control: 30d` | 画像転送を CDN ヒットさせ、egress を抑制。 |
| **非同期**       | Cloud Tasks | サムネ生成・通知送出など *≤10 min* の短時間ジョブ専用。 |
| **監視 / FinOps**| Cloud Monitoring & Budget Alert<br>Billing Export → BigQuery → Looker Studio | • 日次 / 月次アラート<br>• Cloud Run vCPU-sec, Firestore read/write を可視化 |
| **ログ**         | Cloud Logging | 本番ログ **WARN 以上 + 1 % INFO サンプル**<br>保存 7 日。無料枠 50 GiB/月を意識。 |
| **IaC**          | Terraform (import 許容) | 構築スピード優先。変更を後追いコード化 OK。 |
| **レート制限**   | App 中間層ミドルウェア | **60 req/min/IP+UID**。Cloud Armor Basic と併用。 |

---

## 3. UX を守る「防御的設計」＋ コスト監視

| リスク | 防御策 |
|-------|--------|
| **TL が数時間で押し流れる** | ① Cloud Scheduler (1 回/日) で **TL 幅シミュレータ** 実行。<br>② `limit100` で 24 h 未満しか残らない UID > 5 % ならアラート。<br>③ 該当 UID を **ホワイトリスト**し `limit` を 150–200 へ動的昇格。 |
| **Firestore read 課金膨張** | - 上記シミュレータで早期検知。<br>- 閾値越えで **Phase 1: Fan-out on Write** PoC に着手。 |
| **ログ肥大化** | WARN 以上 + 1 % サンプル / 保存 7 日。閾値 ¥0.50/GB 超で自動削除。 |
| **CDN MISS 転送料** | HIT 率を BigQuery で日次集計。**MISS > 20 %** で TTL, 画像圧縮率を調整。 |
| **API 濫用** | アプリ層レートリミット + Cloud Armor Basic。60 req/min 超は 429 返却。 |

---

## 4. 月額コスト試算（2025-07、USD=¥155）

| 項目 | 想定利用 | 月額 |
|------|----------|------|
| **Cloud Run (idle)** | 1 vCPU×512 MiB×720 h | ¥1,500 |
| **Cloud Run (active)** | 300k req × 400 ms | ¥1,500 |
| **Firestore read/write** | 9 M read + α write | ¥3,000 |
| **Firebase Hosting 転送** | 50 GB | ¥1,100 |
| **Cloud Storage GET/PUT** | 30 GB | ¥ 360 |
| **Cloud Logging 超過** | 10 GiB | ¥ 780 |
| **合計 (概算)** |   | **≈ ¥8,200** |

> 無料枠・クレジットは *計算に含めず*、常に実費想定。

---

## 5. SLA / SLO

| エンドポイント | 目標 p95 | 備考 |
|----------------|---------|------|
| `GET /timeline` | < 500 ms | 100 件取得・cursor。 |
| `POST /like` / `POST /post` | < 150 ms | Task 化済み。 |
| `POST /login` | < 800 ms | OAuth リダイレクト含む。 |

Cloud Trace で API パス別に監視し **5 分連続超過**で PagerDuty に通知。

---

## 6. フェーズ移行 & 撤退トリガー

| 指標 | 閾値 | アクション |
|------|------|-----------|
| Firestore read 課金 | ¥300/日 ×3日 | **Fan-out** PoC → Phase 1 準備 |
| p95 レイテンシ | 目標 +30 % ×1週 | **Redis** PoC を別プロジェクトで開始 |
| MAU | 10,000 達成 & 黒字化 | Phase 1 へ移行検討 |
| 月コスト | ¥10,000 超 | 新機能凍結 & コスト削減スプリント |
| 事業健全性 | DAU/MAU < 30 % & 3 か月連続赤字 | **プロジェクト終了** |

---

## 7. セキュリティ最小構成

1. **IAM 最小権限**：サービスごとに専用 Service Account。  
2. **Cloud Armor Basic**：OWASP Top-10 ルールセットをデフォルト ON。  
3. **CI/CD パイプライン**：GitHub Actions → Artifact Registry → Cloud Build (Immutable Deploy)。  
4. **脆弱性スキャン**：Cloud Build で **Trivy** を実行し、重大 CVE で CI 失敗。  

---

### まとめ

- **コスト監視 → 早期アラート → 局所チューニング** の自動ループを回すことで、スモールチームでも「月 ¥10k 生存ライン」を守り切る設計。  
- UX 劣化を招きやすい “limit 100” と “min instances = 1” の両立は、**シミュレーション＋ホワイトリスト緩和** で解決。  
- KPI は **リテンション重視** で PMF を測り、**黒字かつ MAU 1 万** を超えた瞬間に Phase 1 の準備へ。



________________________________________
フェーズ1: 成長と最適化 (1万MAU 〜 10万MAU)
目標: 月3万円以下のコストで安定稼働させ、売上に対するインフラ費比率を15%以下に維持する。
移行トリガー: MAUが1万人に到達し、黒字化の目処が立った時点。
コンポーネント	採用サービス	目的・具体的なアクション
キャッシュ	Memorystore for Redis (1GiB)	「遅いから導入」ではなく、MAU 1万人到達と同時に先行導入。レイテンシ悪化によるユーザー離脱を防ぐ。※MAU 5,000人時点で導入検証(PoC)を完了させておく。
非同期処理	Cloud Tasks	タイムライン生成(Fan-out)、通知、画像処理などを非同期化し、APIの応答性を確保する。
タイムライン	Fan-out on Writeへ移行	Cloud Tasksを使い、投稿時にフォロワーのタイムラインを非同期で事前生成。Firestoreの読み取りコストを劇的に削減する。

重要なアクション
•	Redisの先行検証(PoC): MAU 5,000人の時点で、別プロジェクトでRedis導入時のヒット率、コスト、コールドスタート問題を2週間かけて実測・評価します。
•	非同期基盤の統一: このフェーズではCloud Tasksに一本化し、Pub/Subへの拡張は検討しません。アーキテクチャの複雑化を抑制します。

想定金額 (月額)
•	Cloud Run: ¥7,500
•	Firestore (Read/Write/Storage): ¥10,000 (Fan-outにより書き込み・ストレージ費用は増加)
•	Memorystore for Redis: ¥3,600
•	Cloud Tasks, Storage, Logging等: ¥4,000
•	合計: 約 ¥25,100 (3万円の予算内に収める)


________________________________________
フェーズ2: 拡大と事業価値の最大化 (10万MAU 〜 100万MAU)
目標: 事業価値を最大化し、次の一手（大規模資金調達 or 事業売却）の選択肢を確保する。人的リソース（SRE）の確保が前提条件。
移行トリガー: MAU 10万人達成、かつ事前に定めた定量的KPI（例: 「特定機能のデプロイ頻度が週1回未満になる」など）の未達成。
コンポーネント	採用サービス	目的・具体的なアクション
バックエンド	マイクロサービス化を開始	モノリスのCloud Runを、責務が明確なサービス（ユーザー、投稿など）に分割。
サービス間連携	Pub/Sub	分割したサービス間の疎結合な連携を実現する。
監視	Cloud Trace	マイクロサービス化と同時に必須。サービス横断でのリクエストを追跡可能にする。
データベース	Cloud SQL (検討)	「ユーザープロファイルと認証」など、明確なドメインで強い一貫性が必要な場合に限定導入。その責務は完全にFirestoreから移行し、データストアをまたぐ結合は絶対に避ける。
FinOps基盤	BigQuery + Looker Studio	**「リクエストあたりコスト」「ユーザーあたり売上」**などの経営指標を常時可視化するダッシュボードを構築・運用する。
　
移行の絶対条件
•	SREの確保: この複雑なアーキテクチャを運用するため、最低1名の専任SRE（サイト信頼性エンジニア）の採用、またはチームの組成が必須です。

想定金額 (月額)
•	Cloud Run (複数サービス): ¥37,000
•	Firestore (補助DB): ¥30,000
•	Memorystore for Redis (複数ノード): ¥10,000
•	Cloud SQL (最小構成): ¥10,000
•	その他 (Pub/Sub, 監視費用増大分): ¥10,000
•	合計: 約 ¥97,000 （このコスト増を許容できるだけのARPU向上が必須）

